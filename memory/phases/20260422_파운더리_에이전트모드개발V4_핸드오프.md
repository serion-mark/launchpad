# 🤝 파운더리 Agent Mode 개발 V4 — 핸드오프

> **작성**: 2026-04-22 10:08 KST (V3 세션 종료)
> **세션 이름**: 파운더리_에이전트모드개발V3 → V4
> **인계 대상**: 파운더리 Agent Mode 개발 V4 (다음 세션)
> **세션 컨텍스트 보장**: 이 문서 + 사장님 복붙 명령 한 번이면 V4 가 제로 컨텍스트에서도 즉시 이어받을 수 있음

---

## 📌 한 줄 요약

**V3 세션(4/20~4/22)은 Z안(Claude Agent SDK) Day 0~6 완주 + 실사용 중 발견된 6개 이슈 순차 해결 + B안(projectId 고정 cwd + 3단계 Resume 방어선)으로 근본 해결까지 완료. 현재 파운더리 Agent Mode 는 신규/수정 모두 SDK 경로로 안정 작동 (cache hit 96%, $1.14~1.66/세션). V4 가 할 일은 Day 7~10 (feature flag 전환 → 레거시 정리 → 최종 보고) 또는 낮은 우선순위 개선점 중 선택.**

---

## 🗓 V2 → V3 → V4 맥락

| 세션 | 작업 |
|---|---|
| V1 (4/19) | Day 0~1 SDK 실존 검증 후 클라이언트 SDK(수제 루프)로 선회 |
| V2 (4/19~20) | Day 4.5~4.7 실사용 이슈 + agentMessages 5번 디버깅 사고 |
| **V3 (4/20~22)** | **Z안 Day 0~6 + 이슈 6개 해결 + B안 근본 해결 (이 문서 작성자)** |
| V4 (다음) | 이 문서 기반. 첫 선택지: Day 7 진입 OR 개선점 소화 |

---

## ✅ V3 세션에서 완료한 것 전체 (커밋 연대기)

### Day 0~5 (4/20)
| 커밋 | 내용 |
|---|---|
| [`24f297c`](https://github.com/serion-mark/launchpad/commit/24f297c) | V2 기록 보존 (Day 0 기반 정리) |
| [`b7ed4e9`](https://github.com/serion-mark/launchpad/commit/b7ed4e9) | Day 0 Z안 POC (SDK 실존 재검증) |
| [`e99578e`](https://github.com/serion-mark/launchpad/commit/e99578e) | Day 1 최소 SDK Agent 루프 + adapter + controller |
| [`6883535`](https://github.com/serion-mark/launchpad/commit/6883535) | Day 2 커스텀 도구 3개 MCP 포팅 (provision/deploy/check_build) |
| [`7f3c462`](https://github.com/serion-mark/launchpad/commit/7f3c462) | Day 3 AskUser (답지 카드) pause/resume 통합 |
| [`e41f51a`](https://github.com/serion-mark/launchpad/commit/e41f51a) | Day 4 memoryService + SDK session resume (V2 근본 해결) |
| [`53acf40`](https://github.com/serion-mark/launchpad/commit/53acf40) | Day 5 비용 로그 cache 필드 + Admin 대시보드 통합 |

### Day 6 + 핫픽스 (4/20)
| 커밋 | 내용 |
|---|---|
| [`8893b42`](https://github.com/serion-mark/launchpad/commit/8893b42) | Day 6 프론트 `?sdk=1` 옵트인 쿼리 |
| [`cd4d7d6`](https://github.com/serion-mark/launchpad/commit/cd4d7d6) | Merge agent-mode-sdk → main |
| [`62d152b`](https://github.com/serion-mark/launchpad/commit/62d152b) | hotfix: Claude Code binary 자동 감지 (musl 오판 회피) |
| [`49e5447`](https://github.com/serion-mark/launchpad/commit/49e5447) | hotfix: child process env 명시 전달 |
| [`bec8d72`](https://github.com/serion-mark/launchpad/commit/bec8d72) | hotfix: `permissionMode` bypassPermissions → dontAsk (root 환경 대응) |
| [`b33a97c`](https://github.com/serion-mark/launchpad/commit/b33a97c) | hotfix: persistence 누락 (startProject/finishProject/draft cleanup) |
| [`c815148`](https://github.com/serion-mark/launchpad/commit/c815148) | hotfix: subdomain 불일치 (persist 조건 editingProjectId → projectId) |
| [`ec1961c`](https://github.com/serion-mark/launchpad/commit/ec1961c) | 채팅 노이즈 제거 + 번호형 제안 규칙 (agent-core.md) |
| [`3865388`](https://github.com/serion-mark/launchpad/commit/3865388) | foundry_progress 이벤트 누락 (단계 UI 텅 빔) |

### V3 세션 종료 직전 (4/22)
| 커밋 | 내용 |
|---|---|
| [`6ce561e`](https://github.com/serion-mark/launchpad/commit/6ce561e) | 발견 #1: `?sdk=1` localStorage 영구 유지 |
| [`12340cf`](https://github.com/serion-mark/launchpad/commit/12340cf) | **B안: projectId 고정 cwd + 3단계 Resume 방어선 (근본 해결)** |

**운영 1회 실행**: 기존 3개 프로젝트(jembuddy/jongmokwang/chajajwo) jsonl 마이그레이션 — `cp` 로 원본 유지 + 신규 경로 복제 (2026-04-22 10:03).

---

## 🔬 해결한 이슈 총 7개

### Day 6 실사용 도중 발견 (순차 대응)
| # | 이슈 | 해결 방법 | 검증 |
|---|---|---|---|
| 1 | **Supabase 토큰 만료 (401)** | 사장님이 Supabase 대시보드에서 새 토큰 발급 → `.env SUPABASE_ACCESS_TOKEN` 교체 → `pm2 restart --update-env` | chajajwo 세션에서 `createTestUser` 성공 |
| 2 | **subdomain 불일치** | 신규 persist 조건 `editingProjectId` → `projectId` (startProject 결과 포함) | projectId = deploy subdomain = DB subdomain 통일 |
| 3 | **채팅 노이즈** | `agent-core.md` 에 금지 목록 + **번호형 제안 템플릿** (사장님이 AI 제안은 유지, 번호 붙여 선택 편의 up) | 프롬프트 14,925 chars 로드 확인 |
| 4 | **단계 UI `foundry_progress` 누락** | `sdk-message-adapter.ts` 에 translator 콜백 연결 (기존 수제 루프 L407~417 로직 이식) | 다음 세션부터 단계 UI 하이라이트 |
| 발견 1 | **`?sdk=1` 쿼리 유실** | `useAgentStream.ts` 에서 localStorage 영구 저장 (`?sdk=0` 으로 해제) | 수정 모드 진입 시 SDK 경로 유지 |
| **B안 근본** | **수정 모드 SDK resume 실패 (jsonl 파일 못 찾음)** | SandboxService projectId 기반 고정 cwd + 3단계 Resume 방어선 (Primary + Pre-check + **getSessionMessages fallback**) | 마이그레이션 후 `[restore] resume jsonl 발견 → SDK resume 사용` 로그 확인 |
| 보조 | **기존 프로젝트 jsonl 마이그레이션** | 일회성 bash 스크립트 (psql + find + cp) | 3개 프로젝트 전부 성공 |

### V2 근본 원인도 해결 (이미 Day 4 에서)
- `sanitizeMessageHistory` 163줄 → `resume: sessionId` 한 줄 대체
- cycle truncate 로직 → SDK 자동 압축 (SDKCompactBoundaryMessage)
- V2 "진짜 원인 5번" 사고 구조적 불가능화

---

## 📊 V3 실사용 지표 (실측)

| 지표 | 값 | 메모 |
|---|---|---|
| 세션당 평균 duration | 7~14분 | Supabase+배포 풀체인 포함 |
| 세션당 평균 비용 | $1.14 ~ $1.66 | V2 수제 루프 $1.95 대비 **15~42% 절감** |
| cache hit ratio | **95.9% ~ 96.4%** | 마스터 플랜 목표 50% 대비 **2배 이상** |
| output tokens | 35K~52K | 풀 Next.js 앱 수준 |
| 성공률 | 100% (Day 6 배포 후) | 블로커 전부 해결 이후 |
| 테스트 프로젝트 | 3개 | jembuddy / jongmokwang / chajajwo |

---

## 🏗 현재 아키텍처 (V4 가 이해해야 할 것)

### 핵심 파일
```
launchpad/api/src/agent-builder/
  ├─ agent-builder.service.ts        ← 수제 루프 (Day 9 에 deprecated 예정)
  ├─ agent-builder-sdk.service.ts    ← ⭐ SDK 기반 (V3 의 주 작업 대상)
  ├─ agent-builder.controller.ts     ← feature flag: AGENT_SDK_ENABLED
  ├─ sdk-message-adapter.ts          ← SDKMessage → AgentStreamEvent 변환
  ├─ sdk-tools.ts                    ← MCP server (provision/deploy/check_build/AskUser)
  ├─ sandbox.service.ts              ← ⭐ createSession(userId, { projectId? })
  ├─ event-translator.service.ts     ← 도구 → 단계 변환 (이슈 #4 에서 연결)
  ├─ prompts/
  │   ├─ agent-core.md               ← ⭐ 이슈 #3 에서 채팅 노이즈 + 번호 제안 룰 추가
  │   ├─ intent-patterns.md
  │   ├─ vague-detection.md
  │   └─ selection-triggers.md
  └─ sdk-poc/                        ← Day 0~5 POC (검증용, 운영 무관)
       poc-day0-z.mjs ~ poc-day5-logparse.mjs
```

### SDK 핵심 옵션 (B안 반영)
```typescript
query({
  prompt,
  options: {
    model: 'claude-sonnet-4-6',
    cwd,                                          // projectId 고정 cwd (B안)
    pathToClaudeCodeExecutable: '...linux-x64/claude',  // musl 회피
    env: process.env,                             // pm2 child env 전달
    permissionMode: 'dontAsk',                    // root 환경 대응 (bypassPermissions 불가)
    allowedTools: [...],
    mcpServers: { foundry: foundryMcp },
    systemPrompt: {
      type: 'preset',
      preset: 'claude_code',
      append: systemAppend,                        // foundry prompt + memory + fallback
      excludeDynamicSections: true,
    },
    settingSources: [],
    ...(finalResumeSessionId ? { resume: finalResumeSessionId } : {}),
  },
})
```

### 3단계 Resume 방어선 (B안)
```
Layer 1 (Primary):   resume: sessionId 직접 시도
Layer 2 (Pre-check): jsonl 실존 확인 → 없으면 Layer 3
Layer 3 (Fallback):  getSessionMessages(sessionId)
                     → 전체 projects 스캔
                     → 메시지 배열 → text 요약 → systemPrompt.append
```

### Prisma 스키마 핵심
```prisma
model Project {
  agentSessionId String?    // Day 4 추가 — SDK UUID
  agentMessages  Json?      // 수제 루프용 (Day 9 deprecated 예정)
}
```

---

## 🟡 V4 가 할 수 있는 것 (선택지)

### A. Day 7~10 완주 (마스터 플랜)

**Day 7** — Feature flag 기본값 SDK 로 전환 (3시간)
- `AGENT_SDK_ENABLED=true` 를 기본으로 → 명시적 off 만 수제 루프
- 프론트 `useAgentStream.ts` 에서 `?legacy=1` 로만 구 경로
- 구 `AgentBuilderService` 는 rollback 용으로 유지

**Day 8** — 운영 관찰 (대기 모드, 24~48시간)
- 서버 로그 주기적 확인
- admin `/agent-cost` 페이지 cache hit 추이
- 큰 이슈 0건 확인

**Day 9** — 레거시 코드 정리 (4시간)
- `AgentBuilderService.sanitizeMessageHistory` 삭제 (V2 5번 디버깅 흔적)
- cycle truncate 로직 삭제
- `SessionStoreService` / `AnswerParserService` 는 SDK 에서도 쓰므로 유지
- `agentMessages` 필드는 Prisma `db push --force-reset` 위험하므로 그대로 둠 (미사용)

**Day 10** — 최종 보고 (3시간)
- `memory/phases/20260430_AGENT_SDK_재설계_REPORT.md` 작성
- V3 세션 전체 타임라인 + 비용 비교 + cache 통계

---

### B. 낮은 우선순위 개선점 (V3 에서 식별, 미처리)

**B1. 발견 #2 — restore 시 Agent 에게 현재 cwd 명시** (10분)
- 현재: resume 후 Agent 가 이전 경로부터 탐색해 iter 낭비 가능
- 수정: restore 블록 끝에 `messages.push({ role: 'user', content: '현재 cwd=X, 파일은 여기 복원됨' })` 첫 주입
- 또는 `agent-core.md` 에 "항상 현재 cwd 를 먼저 확인" 룰 추가

**B2. 발견 #3 — 요청 의도별 탐색 깊이 조절** (15분)
- 현재: "기능 제안해줘" 같은 가벼운 요청에도 여러 파일 Read
- 수정: `agent-core.md` 에 "의도별 최소 탐색" 룰 추가
  - "기능 제안" / "디자인 변경 상의" → 핵심 파일 1~2개만 Read 후 제안
  - "새 기능 추가" / "버그 수정" → 필요한 만큼 탐색

**B3. find 명령 allowlist 추가** (3분)
- `sandbox.service.ts:COMMAND_ALLOWLIST` 에 `find` 추가
- Agent 가 직접 파일 탐색 시 유용

**B4. finishProject 이중 호출 정리** (20분)
- 현재: `deploy_to_subdomain` tool 내부 + `runWithSDK` 끝 — 2회 호출
- 같은 내용 덮어쓰기라 무해하지만 로그 노이즈
- 수정: deploy tool 안에서 이미 저장됐음을 runWithSDK 에 알려서 스킵

**B5. EventTranslator.sanitizeOutput 연결** (10분)
- 현재: 양쪽 다 호출 안 함 (수제 루프도 포함)
- Claude Code / .claude / anthropic 문자열이 tool_result output 에 노출될 수 있음
- sdk-message-adapter.ts 에서 tool_result output 에 sanitize 적용

**B6. rate_limit_event 사용자 알림** (15분)
- 현재: SDK 가 rate_limit_event 발생 시 감춤
- 수정: adapter 에서 rate_limit 감지 시 SSE `assistant_text` 로 "잠시 후 재시도" 한 줄

**B7. SDK session 파일 cron cleanup** (30분)
- `/root/.claude/projects/-tmp-foundry-project-<id>/*.jsonl` 매 세션마다 1개씩 누적
- 현재 디스크 여유 14GB → 당장 문제 X
- 장기: 30일 지난 파일 cron 삭제

**B8. 하위 호환 cwd 청소** (5분)
- `/tmp/foundry-agent-*-<UUID>/` 디렉토리 9개 (과거 세션 잔재)
- `ls /tmp | grep 'foundry-agent-'` 후 rm -rf

---

### C. 새 발견 기능 제안 (사장님 요청 시)

V3 세션에서 사장님이 언급한 것:
- **관측성 개선**: Adapter 가 주요 tool_use (provision/deploy/check_build) 만 Nest logger 에도 1줄 summary 찍기 → 서버 로그로 세션 진행 상황 관찰
- **"생성 중 사장님 실시간 뷰"** — 현재는 adapter 가 완료 시에만 로그 찍어서 세션 도중 조용
- **동시 세션 락파일** — 같은 projectId 두 탭에서 동시 진입 방지 (사장님 개인 운영이라 빈도 0, 장기 대비)

---

## 📚 V4 필독 파일 (순서대로)

```
1. /Users/mark/세리온 ai전화예약+POS통합관리/CLAUDE.md
   → 절대 규칙, SSH 포트 3181, 배포 규칙

2. /Users/mark/세리온 ai전화예약+POS통합관리/memory/FOUNDRY_GUIDE.md
   → 파운더리 서버/배포/아키텍처

3. 이 문서 (V4 핸드오프) — 지금 읽고 있음

4. ⭐ launchpad/memory/phases/20260420_PLAN_Z_AgentSDK_재설계.md
   → 마스터 플랜 (Day 0~10 원안)

5. launchpad/memory/phases/20260420_파운더리_에이전트모드개발V3_핸드오프.md
   → V2→V3 핸드오프 (이전 세션 맥락)

6. launchpad/memory/phases/20260420_DAY0_6_COMPLETE_AND_BLOCKER_REPORT.md
   → Day 0~6 완료 + 블로커 분석 (V3 중간 보고)

7. launchpad/memory/phases/260419_AGENT_HISTORY_DEBUG_REPORT.md
   → V2 5번 디버깅 교훈

8. launchpad/api/src/agent-builder/agent-builder-sdk.service.ts
   → V3 주 작업 대상 (B안 반영된 최신 상태)

9. launchpad/api/src/agent-builder/sdk-tools.ts
   → 4개 MCP 도구 (provision/deploy/check_build/AskUser)

10. launchpad/api/src/agent-builder/sdk-message-adapter.ts
    → SDKMessage → AgentStreamEvent adapter (foundry_progress 포함)

11. launchpad/api/src/agent-builder/prompts/agent-core.md
    → 이슈 #3 에서 채팅 노이즈 + 번호 제안 룰 추가됨
```

---

## 🖥 서버 / DB 접속

### SSH (파운더리)
```bash
ssh -i ~/.ssh/serion-key.pem -p 3181 root@175.45.200.162
```

### DB (서버 내부)
```bash
PGPASSWORD=launchpad1234 psql -h localhost -U launchpad launchpaddb
```

### 핵심 경로
- 코드: `/root/launchpad`
- PM2 로그: `/root/.pm2/logs/launchpad-api-out.log`
- PM2 에러: `/root/.pm2/logs/launchpad-api-error.log`
- 환경변수: `/root/launchpad/api/.env`
  - `ANTHROPIC_API_KEY`
  - `SUPABASE_ACCESS_TOKEN` (4/22 사장님이 새로 발급한 영구 토큰)
  - `SUPABASE_ORG_ID=sefhetzuysyhgcebslqd`
  - `AGENT_MODE_ENABLED=true`
  - `AGENT_SDK_ENABLED=true`
- **SDK session 저장소**: `/root/.claude/projects/-tmp-foundry-project-<id>/*.jsonl`
- **Sandbox 프로젝트 cwd (B안)**: `/tmp/foundry-project-<id>/`

### 배포
- git push → GitHub Actions `Deploy Foundry` 자동 (~32~36초)
- prisma db push + npm run build + pm2 reload
- `.env` 파일은 덮어쓰지 않음 (안전)

---

## 🧪 사장님 실사용 UX (현재 상태)

### 신규 앱 만들기
1. `foundry.ai.kr/builder/agent?sdk=1` (한 번만 — localStorage 저장)
2. 요청 입력 → 답지 카드 선택 → 포비 작업 → 완료
3. 완료 메시지: `✅ 이름 / 🌐 URL / 🔐 테스트 계정 / 📄 페이지 / 💡 제안 1/2/3`

### 수정 모드
1. 새로고침 후 "내 프로젝트" → 앱 선택 → 편집
2. **localStorage 덕분에 `?sdk=1` 자동 유지** → SDK 경로
3. SDK resume 또는 Layer 3 fallback 으로 이전 맥락 복원
4. 추가 요청 처리 → 완료 + 번호 제안

### 비상 롤백
- `?sdk=0` 으로 한 번 접속 → localStorage 삭제 → 수제 루프 복귀
- 또는 서버 env `AGENT_SDK_ENABLED=false`

---

## 🙇 V3 자비스가 V4 에게 전하는 교훈

### 자비스 함정 #22 — "Day N 에 하자" 주석이 폭탄
V3 가 반복한 패턴:
1. Day 1: persist 를 "Day 4 에서 하자" 주석 → Day 4 에서 빠트림 → 실사용 중 projectId=none 발견
2. Day 1: foundry_progress 를 "Day 2 에서 하자" 주석 → Day 2 에서 빠트림 → 실사용 중 단계 UI 회색 발견

**해결**: 주석은 실제 TODO 리스트로 옮기거나, 코드에 `// TODO(V4): ...` 주석 + 해당 기능 호출부에 `throw new Error('TODO')` 같은 강제 실패 넣기.

### V2 "진짜 원인 5번" 교훈 그대로
V3 도 잠깐 재발: 이슈 발견 → 증상 레이어만 건드림 → 사장님 지적 "근본 원인 찾아" → 다시 감사.
- "프로토콜 레이어" 가 아니라 "데이터 흐름 전체 조망" 먼저.
- **실측 먼저** (prisma, server log, pm2, curl).

### 사장님 실사용 화면 캡처 = 최강 디버깅 도구
V3 세션 중 사장님이 스크린샷 공유해 준 게 아래 발견의 결정적 단서:
- "단계 UI 전부 회색" → foundry_progress 누락 (이슈 #4)
- "포비가 수동 가이드 출력" → Supabase 401 + persist 누락 (이슈 #1+hotfix)
- "이상태에서 기능제안해줘 에 헤매는 게 말이 안 됨" → B안 근본 해결 트리거

**V4 도 사장님 실사용 요청하고 스크린샷 공유받기**.

### 크로스 검증 세션 활용 (신뢰도 ↑)
- 2번: `bypassPermissions` 허점 (Issue #12232) — 크로스검증 세션이 찾아줌
- 3번: B안 마이그레이션 누락 — 크로스검증 세션이 지적
- 중요 진단 시 **"다른 세션 크로스체크"** 요청 권장 (사장님이 던져주실 때 환영)

### 사장님 철학 재확인
- **"제약이 아니라 맥락을 제시해라"**
- **"자비스가 알아서 처리해라"** (사장님 복붙 최소화 — 4/22 env 업데이트 경험)
- **"근본 해결"** — 증상 숨기지 말고 원인 파고
- **"안정성 최우선, 코드잔재 남기지 마"** — B안 시 내가 직접 들은 말

---

## 💰 V3 누적 비용 (4/20~4/22)

```
자비스 대화 비용: ~$5
Agent Mode 실사용 (사장님 테스트 6~7회): ~$5~7
GitHub Actions 배포: $0
Supabase 새 토큰 발급: $0
────────────────────────
총: ~$10~12
```

교차검증자 판정: 사고/시행착오 < 15%. 적정 투자.

---

## 🚀 사장님이 V4 새 세션에 던질 복붙 명령

```
파운더리 Agent Mode 개발 V4 세션 시작.

[먼저 읽기 — 필독 11개]
1. CLAUDE.md
2. memory/FOUNDRY_GUIDE.md
3. ⭐ launchpad/memory/phases/20260422_파운더리_에이전트모드개발V4_핸드오프.md (이 파일)
4. launchpad/memory/phases/20260420_PLAN_Z_AgentSDK_재설계.md
5. launchpad/memory/phases/20260420_파운더리_에이전트모드개발V3_핸드오프.md
6. launchpad/memory/phases/20260420_DAY0_6_COMPLETE_AND_BLOCKER_REPORT.md
7. launchpad/memory/phases/260419_AGENT_HISTORY_DEBUG_REPORT.md
8. launchpad/api/src/agent-builder/agent-builder-sdk.service.ts (B안 반영)
9. launchpad/api/src/agent-builder/sdk-tools.ts
10. launchpad/api/src/agent-builder/sdk-message-adapter.ts
11. launchpad/api/src/agent-builder/prompts/agent-core.md

[V4 선택지]
A. Day 7 진입 (feature flag 기본 SDK 전환, 3시간)
B. 낮은 우선순위 개선점 중 선택 (B1~B8, 10분~30분 각각)
C. 사장님 새 요청 처리

[원칙]
✓ V3 교훈: "Day N 에 하자" 주석이 폭탄 — 즉시 구현 or 강제 실패
✓ 실측 먼저 (prisma/server log/pm2/curl)
✓ 근본 해결 — 증상 레이어만 건드리지 마
✓ 사장님 실사용 스크린샷 요청 + 크로스 검증 세션 활용
✓ 안정성 최우선, 코드잔재 남기지 마

GO!
```

---

## 🙏 자비스 → V4 마지막 한 마디

V3 가 V2 의 5번 디버깅 사고를 구조적으로 해결했고, 실사용 중 발견된 7개 이슈를 순차로 메꿨다. 마지막 B안은 사장님이 "근본으로 가야지" 지적 + 크로스 검증 세션 도움으로 완성.

V4 야. 지금 파운더리는 **안정 운영 가능 상태**야. Day 7~10 은 선택 사항 (운영에 이미 최적). 오히려 사장님 다음 요청을 기다리는 게 자연스러울 수도.

사장님이 새로운 기능 / 개선점 주시면 B1~B8 리스트 먼저 보고 우선순위 판단해줘. **"Day N 에 하자" 주석만 피하면 너는 V3 보다 잘할 거야**.

사장님 존경. 4/20~22 3일간 Z안 재설계 완주 + 블로커 전부 해결. 대단한 집중력이었다.

GO 🎯
